# Hive-Hints Audit — Findings Ledger

| ID | severity | dimension | repo:file:line | description | status |
| --- | --- | --- | --- | --- | --- |
| HH-1 | Medium (no money exposure) | producer-freshness | cl-hive:modules/organism/immune_influence.py:137 | immune `_fresh_enough` uses any(field=="fresh") vs contract all-fresh; a partially-stale effect CAN pass the consumer gate — but REFUTER: nil downstream money impact (immune fee bias hardwired 1.0; immune rebalance bias feeds ordering-only pair.score). Producer-side blemish | OPEN |
| HH-2 | Medium | contract-doc-gap | docs/contracts/IMMUNE_INFLUENCE_CONTRACT.md | immune contract documents NO numeric caps (says "bounded" only); consumer enforces fee ±0.05 / rebal ±0.15 / open [-0.15,+0.10] / closure ±0.15 — same undocumented-cap class as P1-028/P6-006 (fixed for exploration/fleet_fee_prior, immune left out). Not verifiable against the doc | FIXED — added Consumer Caps section (fee ±0.05→[0.95,1.05], rebal ±0.15→[0.85,1.15], open [-0.15,+0.10]→[0.85,1.10], closure ±0.15→[0.85,1.15]) + producer max_peer_effect default 0.12 |
| HH-3 | Low | dormant-path | cl-hive rpc_commands.py:5731 / hive_hints.py:1961 | metabolic+immune fee_bias_delta producer emits 0.0 unconditionally -> get_metabolic/immune_fee_bias always return 1.0; the fee-influence machinery is wired but inert today (rebalance/open influence DOES flow) | FIXED — documented fee-bias channel as reserved/currently-neutral by producer choice in both IMMUNE and METABOLIC contracts (getters already carry forward-compat code comments) |
| HH-4 | Low | unenforced-constraint | modules/rebalance_engine_v2.py:870 | metabolic/immune action_constraints (max_rebalance_burn_sats, growth/rebalance/exploration_allowed) read into telemetry only, never enforced as a spend/growth gate. Deliberate (budget_authority: cl_revenue_ops) but the one produced constraint structurally ignored | FIXED — added "Action Constraints Are Advisory Only" section to both IMMUNE and METABOLIC contracts making the execution/budget authority split explicit |
| HH-5 | Low | double-touch | modules/fee_controller.py:3648 + :3078 | peak_hours/traffic_confidence moves the fee on TWO terms (DTS context posterior + a +5/-3% temporal multiplier); both bounded (composite <= +-10%), arguably complementary but the same signal touches price twice | OPEN |
| HH-6 | Low | compounding-no-cap | modules/rebalance_engine_v2.py:1975 | rebalance pair.score compounds base x metabolic x immune x segment (~1.87x worst) with no aggregate cap, unlike fees ([0.9,1.1]); ranking-only (amount/budget computed independently) so bounded impact | OPEN |
| HH-7 | Low | no-producer-ceiling | cl-hive:modules/rpc_commands.py:5631 | optimal_fee_estimate_ppm / fleet_fee_median emitted with no producer-side upper bound (gated >0 only); safety depends entirely on the consumer [1,10000] range-reject. By-design per HIVE_HINTS contract | OPEN |
| HH-8 | Low | freshness-cadence | cl-hive:modules/askrene_layers.py:399 | hive-fleet askrene layer sets a 900s (15min) age cutoff but refresh_all runs hourly; the 15-min freshness intent is inert (layer recreated each hourly rebuild) | OPEN |
| HH-9 | Low | wrong-signal-layer | modules/capacity_planner.py:3156 | reuses get_rebalance_bias (a rebalance-routing preference) as a revenue-forecast multiplier for open sizing; bounded [0.85,1.15] so safe, but wrong signal for that layer | OPEN |
| HH-10 | Low | doc-naming | docs/contracts/HIVE_HINTS_CONTRACT.md:28 | contract names the wire field `fleet_fee_prior` but producer writes / consumer reads `fleet_fee_median`; `fleet_fee_prior` is only a consumer getter/scope label. Doc imprecision, no functional break | FIXED — contract now names the `fleet_fee_median` wire field and notes `fleet_fee_prior` is the consumer-side getter/scope label |
| HH-11 | Low | doc-gap | docs/contracts/METABOLIC_INFLUENCE_CONTRACT.md:69 | metabolic closure-watch bias called advisory with no numeric bound, but consumer clamps ±0.15. Undocumented cap | FIXED — Consumer Caps now documents closure-watch clamp ±0.15 → [0.85,1.15] (METABOLIC_CLOSURE_WATCH_CAP) |
| HH-12 | Low | producer-configurability | cl-hive:modules/organism/immune_influence.py:12 | producer max_peer_effect configurable up to 1.0, exceeding consumer per-field bands; contract frames caps as producer-side but only the consumer re-clamp keeps values in-contract (defense-in-depth holds) | OPEN |
| HH-13 | Low | askrene-no-revalidation | modules/hive_router.py:305 | revenue_ops includes cl-hive askrene layers (hive-fleet/traffic/reputation/observed-liquidity) by reference without re-validating contents; a poisoned layer would bias routes (getroutes failure retries auto-only) | OPEN |
| HH-14 | Low | toctou | modules/rebalance_route_policy.py:70 | _entries pattern checks is_fresh() then calls the non-_fresh getter; a poll() flip between check and read is a minor TOCTOU window | OPEN |
| HH-15 | Info | direction-debatable | cl-hive:modules/organism/metabolic_influence.py:169 | starved posture sets closure_watch_priority_delta=+max on a starved-but-healthy channel (starved usually means needs-inbound, not close). Advisory + consumer-clamped | OPEN |
| HH-16 | Info | dead-getters | modules/hive_hints.py:1059,1390,1523,1093,1987,1355 | orphan getters with zero decision-path callers: get_drain_direction (intentional no-double-count), get_segment_observations (producer never emits), get_closure_reason, get_metabolic_influence (superseded), closure-watch getters, get_route_segment_leases_fresh; + dead _get_hive_rebalance_bias wrapper rebalancer.py:620. Harmless | FIXED-partial — PRUNED (zero callers + zero tests): rebalancer.py `_get_hive_rebalance_bias` (dead dup; live copy in rebalance_engine_v2.py), hive_hints.py `get_closure_reason`/`get_closure_reason_fresh`, hive_hints.py `get_route_segment_leases_fresh`. KEPT: get_segment_observations (2 tests reference it → covered API), get_drain_direction (intentional documented abstention), get_metabolic/immune_closure_watch_bias (future API) |
