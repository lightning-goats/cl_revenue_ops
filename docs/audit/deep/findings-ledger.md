# Deep-Audit Findings Ledger

Per P6 of the deep-audit campaign. One row per finding. `file:line@blob` pins
the finding to a specific line and the git blob it was found in, so coverage
accounting (`tools/audit/deep_manifest.py --coverage`) can map the finding onto
its chunk. A chunk that contains a cited line counts as COVERED by that finding.

`status` values: `OPEN`, `FIXED`, `BEHAVIORAL-HOLD`, `WONTFIX`, `DUPLICATE`.
`severity`: `Critical`, `High`, `Medium`, `Low`, `Info`.
`dimension`: one of the seven never-audited dimensions or `line-audit` /
`docs-conformance` (see the plan).

Rows whose ID contains `EXAMPLE` are ignored by the coverage tool.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| EXAMPLE-000 | Low | line-audit | modules/utils.py:42@e96115ef457c35700b65329422a01584c43ff6ba | EXAMPLE ROW ONLY — illustrates the citation format; ignored by --coverage. Replace with real findings. | OPEN | | |

## Phase 1 — cl-revenue-ops.py + RPC security + hive boundary (2026-07-02)

Blobs: cl-revenue-ops.py@acbdb42, hive_hints.py@9e5eec5, rebalancer.py@d424e24, database.py@d96b136, boltz_manager.py@b740d8b. Verdicts below are pre-refutation-gate.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-001 | Medium | security/money-path | cl-revenue-ops.py:3648@acbdb42 | `revenue-rebalance` force=false is the UNGATED path: manual_rebalance (rebalancer.py:2564) bypasses daily-budget + wallet-reserve, no rate limit (ForceRateLimiter only when force=true), no amount cap. REFUTED-DOWN High→Medium: operator-only + documented-by-design (rebalancer.py:2551), bounded by liquidity — same class as P1-006 which was Medium. Novel part = rate-limit asymmetry (force=false LESS gated than force=true), itself Low. DD2: force=false rate-limited like force=true; hard rebalance amount cap binds under force. | FIXED | 96467a2 | tests/test_force_ceilings.py |
| P1-002 | Low | concurrency | cl-revenue-ops.py:2618@acbdb42 | No single-flight lock on run_rebalance_check/run_fee_adjustment/run_flow_analysis at this layer; reachable from daemon+RPC threads. REFUTED-DOWN High→Low: money path already single-flighted one layer down — RebalanceEngineV2._cycle_lock (rebalance_engine_v2.py:228, non-blocking at :3120/:3247 → cycle_already_running/engine_busy) and fee _state_lock (fee_controller.py:2591). Defense-in-depth, not a live race. | WONTFIX | | |
| P1-003 | Medium | concurrency/financial | rebalancer.py:1974@d424e24 | Cross-category budget reserve is a TOCTOU: the external-liquidity read (_get_external_liquidity_costs, rebalancer.py:1974-1977) that pre-subtracts boltz+ledger happens OUTSIDE the BEGIN IMMEDIATE txn, and the generic path (revenue-spend-reserve, cl-revenue-ops.py:6036) reads rebalance reservations at a different instant under a different lock. REFUTED-DOWN High→Medium: rebalance IS cross-category-aware (not disjoint as first claimed); DEF-087 attribution REFUTED (those 129 snapshots were operator mass-close on-chain fees, reserved cats=0, loop correctly suppressed per rebalance-loop.md LP-I1a/c). Real but latent, bounded by daemon cadence. CARRY TO PHASE 2A. DD1: both reserve paths enforce a live cross-category total inside BEGIN IMMEDIATE (serialized by WAL writer); no memo on gating. Stress sentinel now passes. | FIXED | 59991ae | tests/test_cross_category_budget_atomicity.py |
| P1-004 | Medium | security/money-path | cl-revenue-ops.py:3593@acbdb42 | `revenue-set-fee force=true` sets enforce_limits=False, skipping [min_fee_ppm,max_fee_ppm]; fee_ppm only checked >=0 (no upper cap ever); fee_ppm=0 reachable (Red Team 1ppm-floor concern). Only guard is 10/60s rate limit. DD2: fee clamped to [min,max] + absolute ceiling even under force; fee_ppm can't go below min. | FIXED | 96467a2 | tests/test_force_ceilings.py |
| P1-005 | Medium | resource | cl-revenue-ops.py:3561@acbdb42 | `revenue-planner-candidates` / `revenue-planner-history` pass `limit` unclamped into SQL `LIMIT ?`; SQLite treats LIMIT -1 as unlimited → whole-table return (memory DoS) on a read-only "Hermes-safe" surface; non-int → uncaught error. Also cl-revenue-ops.py:3589. _clamp_query_limit → [1,1000] before SQL. | FIXED | 28099c9 | tests/test_rpc_param_validation.py |
| P1-006 | Medium | security/money-path | cl-revenue-ops.py:6237@acbdb42 | `revenue-boltz-withdraw` moves on-chain funds with NO destination address-format validation, NO amount upper bound, NO budget gate (boltzcli is sole validator). Operator-only (no remote reachability) → Medium not Critical. ADDRESS-VALIDATION FIXED (9637c54, inline bech32/bech32m/base58check BTC+Liquid, no new dep); amount-cap + budget-gate remain BEHAVIORAL-HOLD. DD3: revenue-ops-boltz-max-withdraw-sats cap (default 10M); sweep requires confirm_sweep=true. (address validation was 9637c54). | FIXED | 42594cb | tests/test_boltz_withdraw_cap.py |
| P1-007 | Medium | error-path/resilience | cl-revenue-ops.py:382@acbdb42 | RPC proxy has no socket-level timeout; a wedged lightningd permanently consumes all 16 worker threads (they block on recv()); thereafter every RPC raises RPCOverloadedError. Plugin goes RPC-dead with no self-recovery. Futures/slots themselves do NOT leak (released once). Socket-level recv timeout (pyln UnixSocket.connect patch, graceful degrade; caller-future timeout backstop). | FIXED | 40f2074 | tests/test_rpc_param_validation.py |
| P1-008 | Medium | security/input-validation | cl-revenue-ops.py:1757@acbdb42 | Init option parsing validates type not range: rpc-timeout-seconds=0 → every future.result(timeout=0) fails instantly (total RPC failure); negative daily/weekly budget accepted; reputation_decay/htlc_congestion_threshold unclamped to [0,1]. Fail-open. Lines 1757/1764/1767/1769/1774. _validate_numeric_config_options clamps at init (warn). | FIXED | 3dd525e | tests/test_rpc_param_validation.py |
| P1-009 | Medium | correctness/input-validation | cl-revenue-ops.py:1709@acbdb42 | No min_fee_ppm <= max_fee_ppm invariant at init; inverted bounds silently pin fees to a low ceiling and suppress revenue (no crash/warning). _enforce_fee_bound_invariant swaps inverted min/max (warn). | FIXED | 56f107f | tests/test_rpc_param_validation.py |
| P1-010 | Medium | daemon-survival | cl-revenue-ops.py:2204@acbdb42 | Per-iteration sleep/interval/config.snapshot() tail of every daemon loop is OUTSIDE the loop-body try/except; an exception there kills the thread permanently and silently (daemon, no respawn, no heartbeat). Pattern repeats 2234/2266/2318/2369. CARRY TO PHASE 2C (fault-injection + heartbeat). DD5: all 7 loops guard the full iteration incl tail + bounded backoff; per-thread heartbeat on revenue-health; survival tests flipped. | FIXED | 13955de | tests/test_daemon_survival.py |
| P1-011 | Medium | resource/DoS | modules/hive_hints.py:242@9e5eec5 | Consumer applies no size/entry-count cap before json.loads + _normalize_hint_map (dict-copies every peer) + repeated O(n) scans; a hostile multi-MB / 100k-entry ["hive","hints"] value degrades the fee daemon every poll(). Contract says consumer must not trust producer-side caps. Consumer-side byte+entry caps (900000B / 200 peers) checked O(1) before parse. | FIXED | 8a4651c | tests/test_hive_hints*.py |
| P1-012 | Low | error-path | cl-revenue-ops.py:3485@acbdb42 | Type-confusion: re.match()/int() run on raw operator params outside try/except in several handlers (analyze:3485, set-fee:3618, rebalance:3672, hive-hints-status:2805, dashboard:4631) → uncaught TypeError/ValueError leaks a Python traceback instead of a clean error dict. isinstance/try guards; +3 more class-instances fixed in 8e591b2 (rebalance-debug max_candidates, config non-str key, balance-cycle cooldown_hours). | FIXED | d69048a | tests/test_rpc_param_validation.py |
| P1-013 | Low | concurrency | cl-revenue-ops.py:358@acbdb42 | Proxy timeout is not cancellation: a timed-out future is abandoned (not future.cancel()'d) and may still execute later. Known proxied callers (sendpay/setchannel) are idempotent → low impact; latent double-execution hazard for any non-idempotent proxied call. | OPEN | | |
| P1-014 | Low | error-path/shutdown | cl-revenue-ops.py:1627@acbdb42 | atexit _shutdown_cleanup shuts pools then closes DB connections, but the 7 daemon threads are never joined (daemon=True); a daemon mid-DB-write can hit a connection closed under it at exit (sqlite misuse/torn write). Bounded to process exit. | OPEN | | |
| P1-015 | Low | security/injection | modules/boltz_manager.py:1888@b740d8b | Free-form params passed as bare positional argv to boltzcli with no `--` end-of-options terminator (withdraw dest:1888, refund:1826, claim:1834, loop_out addr:1555/1671, swap_status:1761); a value starting with `-` is reparsed as a flag. NO shell injection (list argv). Operator-only. `--` end-of-options terminator before free-form positionals. | FIXED | 37f4e99 | tests/test_boltz_manager*.py |
| P1-016 | Low | money-path/bounds | modules/boltz_manager.py:1340@b740d8b | Direct loop-out/loop-in/chainswap RPCs bound amount only by >0 (no upper cap); the plan path caps at 25%/5M but the direct operator RPC does not. Loss bounded by fee-budget gate + liquidity. Lines 1340/1376/1457/1839. | OPEN | | |
| P1-017 | Low | authority/config | modules/boltz_manager.py:1313@b740d8b | chainswap's only budget guard is the fee-check (_enforce_budget_for_quote), a no-op when enforce_budget=false; unlike loop_in/loop_out it has no tactical/capex gate. | OPEN | | |
| P1-018 | Low | authority | cl-revenue-ops.py:7901@acbdb42 | `revenue-boltz-auto-cycle-run-now force=true` runs a LIVE cycle (dry_run=False) bypassing the boltz_auto_cycle_enabled toggle; no dry_run preview exposed. Economic gates still apply (force bypasses enable only). DEF-005 family. DD4: dry_run param (default true, safe preview); force+dry_run=false runs one live cycle. | FIXED | 5520835 | tests/test_boltz_auto_cycle_dry_run.py |
| P1-019 | Low | authority/econ | cl-revenue-ops.py:4189@acbdb42 | `revenue-policy set` passes fee_ppm_target / fee_multiplier_min/max with no numeric bounds at the RPC layer; min=0/negative collapses a peer's dynamic fee floor. Requires internal/admin write override → Low. | OPEN | | |
| P1-020 | Low | concurrency/config | cl-revenue-ops.py:725@acbdb42 | on_change / _refresh_dynamic_config writers (725, 1190, 5244) mutate live config bypassing config._lock and never bump _version; safe today only because each is a single GIL-atomic scalar; a future multi-field on_change reintroduces torn reads (C-3). CARRY TO PHASE 2A. | OPEN | | |
| P1-029 | Low | error-path/shutdown | cl-revenue-ops.py:1627@acbdb42 | (refuter NEW-A) _shutdown_cleanup calls executor.shutdown(wait=True, cancel_futures=True); cancel_futures only drops QUEUED futures, in-flight workers blocked on a wedged lightningd recv() can't be cancelled, so atexit blocks indefinitely (relies on external SIGKILL). Inline comment claiming proxy timeouts bound it is wrong (they bound the caller, not the worker). _bounded_executor_shutdown drains on daemon thread w/ timeout join. | FIXED | 0cccf32 | tests/test_p1_029_bounded_shutdown.py |
| P1-030 | Low | correctness/idempotency | modules/database.py:3753@d96b136 | (refuter NEW-B) reserve_spend does INSERT OR REPLACE keyed on reservation_id with hardcoded status='active' and no status guard; re-submitting an already-spent/released reservation_id resurrects it to active, bypassing the release/settle audit paths and double-counting (spend_event persists while reservation re-counts as reserved). Direction is conservative (over-counts, can't overspend). RPC surface cl-hive/boltz drive. Status guard rejects re-reservation of terminal reservation_id. | FIXED | 86014e1 | tests/test_database*.py |
| P1-021 | Low | resource/FD | cl-revenue-ops.py:2391@acbdb42 | snapshot_peers_delayed one-shot thread opens a thread-local SQLite connection and exits without close_connection(); connection stays referenced until shutdown. Bounded (startup-only). In-range instance of DEF-007/021. _snapshot_peers_once closes thread-local conn in finally. | FIXED | f0e12df | tests/test_p1_021_snapshot_conn.py |
| P1-022 | Low | freshness/validation | modules/hive_hints.py:1507@9e5eec5 | Metabolic/immune section TTL is max(0,int(ttl)) with no upper clamp (outer snapshot clamps to 86400); a huge section TTL defeats section-freshness while the snapshot stays fresh → stale arbitration applied. Effect bounded by [0.85,1.15]+rails. Section TTL clamped to HINT_MAX_TTL_SECONDS=86400. | FIXED | 1909664 | tests/test_hive_hints*.py |
| P1-023 | Low | validation | modules/hive_hints.py:1035@9e5eec5 | get_optimal_fee_estimate has no upper bound (int(val) for val>0), inconsistent with get_fleet_fee_prior's [1,10000]. Only consumer is a debug dict → latent (no money path today). Bounded to [1,MAX_FLEET_FEE_PRIOR_PPM]. | FIXED | 95a302e | tests/test_hive_hints*.py |
| P1-024 | Info | authority | cl-revenue-ops.py:4995@acbdb42 | clear-reservations / cleanup-closed have no confirm token or rate limit; matches budget_mutation/mutation classification. Noted; abusing clear-reservations then rebalancing compounds P1-001. | WONTFIX | | |
| P1-025 | Info | security | modules/boltz_manager.py:1931@b740d8b | backup_verify uses non-constant-time == (impractical timing side-channel over operator RPC); backup(include_mnemonic=true) returns plaintext mnemonic (documented sensitive_read, may reach logs). | WONTFIX | | |
| P1-026 | Info | input-validation | cl-revenue-ops.py:1711@acbdb42 | Enum-style string options (market_fee_mode/base_fee_policy/fee_profile/preferred_currency) not membership-validated; a typo silently falls through to default behavior with no warning. _validate_enum_config_options warn+default on typo. | FIXED | f3c4afd | tests/test_p1_026_enum_options.py |
| P1-027 | Info | trust-boundary/docs | modules/hive_hints.py:557@9e5eec5 | M2 scope for opens/metabolic/immune/closure keys on producer-asserted member=true / direct_channel_peer=true; a malicious cl-hive self-asserts to bring payloads in-scope (bounded by capex/score/gates). Designed model, but direct_channel_peer is locally verifiable and is not cross-checked. | OPEN | | |
| P1-028 | Info | docs-conformance | modules/fee_controller.py:2930@8463adc | Exploration multiplier [0.75,2.0] and fleet fee prior [1,10000] are rail-bounded, NOT the ±10% "bounded hive authority" the README/contract advertise. Re-confirms FC-I6 / DEF-073 / DEF-092. Phase 6 doc fix. | OPEN | | |

## Phase 2 — concurrency + daemon survival (2026-07-02)

Blobs at time of analysis: cl-revenue-ops.py@acbdb42, flow_analysis.py, rebalancer.py@d424e24, fee_controller.py@8463adc, database.py@d96b136. Map: docs/audit/deep/concurrency-map.md. C-1 (autocommit corruption) CLOSED-BY-MECHANISM; C-3 (config torn read) CLOSED-BY-MECHANISM (residual = P1-020 Low); no deadlock found.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-001 | Medium | concurrency | modules/flow_analysis.py:1675@map | RPC analyze_channel (1675) skips _analysis_lock, and kf.predict()/kf.update() (961/968) mutate the Kalman filter with no lock (_kalman_lock guards only the dict lookup 821/838). revenue-analyze on the dispatch thread races the flow daemon on the same filter → double-applied/torn observations (DEF-027/029). Non-behavioral fix: take _analysis_lock on the RPC path / lock the filter mutation. Kalman mutation under widened _kalman_lock; analyze_channel takes _analysis_lock (bounded fail-open 5s). | FIXED | 408db58 | tests/test_p2_concurrency_hardening.py |
| P2-002 | Medium | resource/DB | modules/database.py:6272@d96b136 | "database is locked" reachable: long BEGIN IMMEDIATE batches — cleanup_old_data (6272, every flow cycle) and bulk_insert_forwards (4441) — can hold WAL's single writer > busy_timeout=5000ms while another thread (record_fee_change 2249 / record_forward) contends → OperationalError. Non-behavioral fix: chunk the batches / shorten the lock hold. cleanup_old_data + bulk_insert_forwards chunked (per-chunk commit); crash-consistency + idempotency preserved. | FIXED | 78d7586 | tests/test_p2_concurrency_hardening.py |
| P2-003 | Low | correctness/crash-consistency | modules/database.py:3789@d96b136 | Spend-ledger multi-statement writes (reserve_spend 3736, mark_spend_reservation_spent 3789, release_spend_reservations 3881) do read-then-write with no BEGIN; a crash between UPDATE and record_spend_event leaves a window (idempotency guards keep it consistent-not-corrupt). Fix: wrap in a transaction. spend-ledger read-then-write pairs wrapped in BEGIN IMMEDIATE; mark_spent rolls back if event write fails. | FIXED | e5b99e0 | tests/test_p2_concurrency_hardening.py |
| P2-004 | Low | resource/DB | modules/database.py:4204@d96b136 | Deferred BEGIN (not IMMEDIATE) at 4204 → higher SQLITE_BUSY probability under write contention (writer lock acquired late). Fix: BEGIN IMMEDIATE for the write path. get_budget_status uses BEGIN IMMEDIATE (values identical on healthy path). | FIXED | 773ec48 | tests/test_p2_concurrency_hardening.py |
| P2-005 | Low | concurrency | modules/rebalancer.py:1697@d424e24 | _fee_cache (305/1697/1740, reset 1387) and _peer_inbound_fees (1704/1816/1820) mutated with no lock; T3 rebalance daemon vs T0 manual rebalance (force=false bypasses engine cycle) → torn cache. DEF-061/DEF-067-S9. Fix: guard with a lock. _fee_cache/_peer_inbound_fees guarded by a lock. | FIXED | 70b725a | tests/test_p2_concurrency_hardening.py |
| P2-006 | Low | concurrency | modules/fee_controller.py:2604@8463adc | _hive_member_set_at/_hive_member_advisory_peers/_hive_member_released_peers (2604-2606) set/dict mutated lock-free by the fee cycle (T2) and hint/forward_event paths (T0); set-resize-during-iteration risk on the zero-fee hive gate. Fix: lock or iterate a copy. hive-member set/dict under dedicated _hive_member_lock (no cycle: _state_lock→_hive_member_lock only). | FIXED | 21cd6a0 | tests/test_p2_concurrency_hardening.py |
| P2-007 | Low | concurrency | modules/fee_controller.py:3671@8463adc | Lock-free _channel_fee_states reads (3671/4969/6559/6651/6706/6729/6787/6867) while the cycle writes it under _state_lock → stale/torn reads in operator output (not a money path). Fix: read under lock or snapshot. channel-fee-state reads use atomic .get() snapshot (no coupling to long cycle _state_lock); residual field-level cosmetic Low. | FIXED | 85309d6 | tests/test_p2_concurrency_hardening.py |

## Phase 2 — verdict refutation gate additions (2026-07-02)

Refuter CONFIRMED C-1 (no corruption escalation), no-deadlock, and P2-001; partially refuted the C-3 supporting reasoning and found 3 new Lows. Blobs at HEAD 8569eeb.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-008 | Low | correctness/lost-write | modules/database.py:3841@8569eeb | record_spend_event (3841) and reserve_spend (3776) catch Exception→return False; caller mark_spend_reservation_spent (3809) ignores the return and reports success on the UPDATE alone. A "database is locked" (reachable per P2-002) on the record_spend_event INSERT is a SILENTLY LOST write, never retried (reservation already 'spent', guard rowcount 0). Under-counts generic-ledger spend → remaining higher → toward permitting overspend (dangerous direction). Bounded to generic-ledger category (boltz/rebalance use canonical cost sources). Refutes the "idempotency keeps it consistent" claim in P2-003. DD1: record_spend_event retries OperationalError then propagates; mark_spend_reservation_spent rolls back on lost event. | FIXED | 59991ae | tests/test_cross_category_budget_atomicity.py |
| P2-009 | Low | concurrency/TOCTOU | modules/database.py:3757@8569eeb | The P1-030 terminal-status guard (SELECT status 3757-3769 → INSERT OR REPLACE 3770) is non-atomic across two autocommit statements; a concurrent mark_spend_reservation_spent between the SELECT and INSERT resurrects a 'spent' reservation to 'active' (over-counts reserved — conservative direction). Reachable on multiple threads (cro:6407/6480, capacity_planner:3311/3367/3664/3679). Fix: fold the guard into a single transaction. | OPEN | | |
| P2-010 | Low | concurrency/config | modules/flow_analysis.py:1108@8569eeb | FlowAnalyzer holds the LIVE global Config (cro:2361, self.config never reassigned to a snapshot) and reads the source_threshold/sink_threshold invariant pair as two separate live reads (1108-1109, 1903-1905) on the T1 flow daemon, racing update_runtime on T0. Refutes the concurrency-map claim that all invariant pairs are read via snapshot(). Torn read → transient channel SOURCE/SINK/BALANCED misclassification; self-corrects next cycle. Money-safety conclusion unaffected. Fix: read the pair off a single snapshot. | OPEN | | |

## Phase 2 — 30-min soak result (2026-07-02)

30-minute soak (14.1M ops) completed: integrity_check PASS, no-deadlock PASS, no-thread-exception PASS, budget-never-exceeded FAIL. P1-003 reproduced; the dominant amplifier is a 30s memoization (below), which makes overspend reproducible even at LOW concurrency — CORRECTING the earlier "synthetic magnitude / small multiple" assessment. P1-003 realistic severity is accordingly higher (a 30-second blind window, not a tight TOCTOU).

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P2-011 | Medium | concurrency/money-path | cl-revenue-ops.py:6833@8569eeb | revenue-spend-reserve's budget gate (6388-6415) checks _total_cost_budget_status(), which is MEMOIZED 30s (_TOTAL_COST_BUDGET_MEMO_TTL_SECONDS=30, 6833/6859-6884). N reservations within one TTL window all pass against the same stale remaining_sats → unbounded overspend independent of thread count. The code comment claims budgets are "enforced atomically at reservation time," but Database.reserve_spend does not enforce budget and the caller reads a stale cache. Dominant amplifier of P1-003 (soak reached 842x budget). Fix (BEHAVIORAL — bundle with P1-003 ruling): enforce the budget inside the reservation transaction against a live cross-category total, not a memoized snapshot. DD1: 30s memo removed from the reservation gating path (force_fresh); retained for non-gating telemetry only. | FIXED | 59991ae | tests/test_cross_category_budget_atomicity.py |

## Phase 3A — migration integrity (2026-07-02)

modules/database.py uses "run all idempotent DDL on every startup" (schema_version table is write-only, never read/bumped). 9 historical schema shapes anchored from git history. test_migrations.py (22 tests) added.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| MIG-1 | Medium | migration | modules/database.py:1342@07356f0 | _migrate_forwards_schema (called ~:701) backfills resolution_time BEFORE the ALTER ADD resolution_time (~:1087); on a pre-2025-12-19 DB the UPDATE throws no-such-column, the BEGIN IMMEDIATE rolls back (undoing the resolved_time ADD + idx_forwards_unique dedup index, warn-only), so record_forward then fails no-such-column resolved_time → forward-revenue recording fully broken until the SECOND restart. Node 1 may run a pre-column DB. Fixed order-independent (ensure column exists in the migration before backfill). | FIXED | 6ebcd53 | tests/test_migrations.py |
| MIG-2 | Low | migration | modules/database.py:1245@07356f0 | pid_state table (PID controller, replaced by v2_state_json) removed from CREATE but never dropped, unlike flow_history/portfolio_metrics/clboss_unmanaged; migrated DBs permanently carry an orphan table fresh installs lack (fresh != migrated). Fixed: DROP TABLE IF EXISTS pid_state (:1256). | FIXED | 6ebcd53 | tests/test_migrations.py |
| MIG-3 | Low | migration | modules/database.py:600@07356f0 | schema_version is write-only (seeded to 1, never read/bumped); zero version gating — a DB written by a future (newer-schema) code version opens with no refusal. Low-risk today (all migrations additive/idempotent) but a latent trap once a migration becomes destructive/renaming. Real version gating is behavioral (could brick a rolled-back node) → operator ruling. Behavior locked by a documenting test. DEFERRED TO PHASE 8 ruling. | BEHAVIORAL-HOLD | | tests/test_migrations.py |

## Phase 3B — resource / DB growth (2026-07-02)

T0 "fee_changes unbounded" lead CORRECTED: fee_changes is BOUNDED (90d retention via cleanup_old_data every flow cycle, at steady state). No table on a path to 1GB in any relevant horizon. Doc: docs/audit/deep/resource-growth.md.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RES-1 | Low | resource | cl-revenue-ops.py:850@76e7c52 | _boltz_balance_last_action per-channel cooldown map had no eviction (only unbounded in-memory structure found; all other caches close-evict/TTL/size-cap/rebuild). Fixed: 30d TTL + _prune_boltz_balance_actions at both balance-cycle executors (TTL dwarfs the 4h cooldown, non-behavioral). | FIXED | 98b3f30 | tests/test_boltz_balance_action_eviction.py |
| RES-2 | Low | resource | cl-revenue-ops.py:6551@76e7c52 | fee_changes retention (90d) works but is the one lever if the node scales up (linear; ~100x node -> 1-2GB from this audit-log table). Recommend 30d and/or roll counts into an aggregate before delete (mirrors forwards->daily_forwarding_stats). Data-deletion policy change = behavioral. DEFERRED TO PHASE 8 ruling. | BEHAVIORAL-HOLD | | |
| RES-3 | Low | resource | modules/boltz_manager.py:1425@8db9376 | Boltz swap-journal file dedups by swap id but has no count/age cap; grows per swap on disk. Recommend keep-last-N / 180d. Behavioral (history trim). DEFERRED TO PHASE 8 ruling. | BEHAVIORAL-HOLD | | |

## Phase 3C — supply chain (2026-07-02)

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SC-1 | High | supply-chain | requirements.txt@d0abbf4 | Runtime deps unpinned (pyln-client>=, PyYAML>=, numpy bare) → a reinstall/restart could pull an incompatible version and break the plugin; non-reproducible installs. FIXED: exact pins to the runtime interpreter (pyln-client==25.12.1, PyYAML==6.0.1, numpy==1.26.4) + requirements.lock (hash-pinned) + CycloneDX SBOM. PRODUCTION VERSION VERIFICATION REQUIRED before deploy (pins from dev runtime; SBOM/lock regenerate from prod). | FIXED | e31f0f6 | tests/test_supply_chain_pins.py |
| SC-2 | Medium | dependency-completeness | tools/audit/contrib_fee_analysis.py:63@campaign | Audit tools import scipy.stats but scipy is neither declared nor installed → ModuleNotFoundError in a clean env. Audit-only (not runtime); documented in requirements.txt. | OPEN | | |
| SC-3 | Low | missing-version-probe | cl-revenue-ops.py:init@76e7c52 | No startup check of the CLN v24.11.1 floor or boltzcli minimum despite hard askrene-v3 / boltzcli-argv dependence → a downgraded CLN/boltzcli fails deep with opaque errors. FIXED: non-fatal warn-only probes at init. | FIXED | 7e60848 | tests/test_supply_chain_pins.py |
| SC-4 | Info | supply-chain | modules/boltz_manager.py:55@8db9376 | Positive confirmation: Phase-1 inline bech32/base58 reimplementation means those deps are NOT imported anywhere; correctly excluded from requirements. Enforced by test_bech32_base58_not_imported_anywhere. | FIXED | e31f0f6 | tests/test_supply_chain_pins.py |

## Phase 4 — Tier-1 line-by-line adversarial re-audit (2026-07-02)

5 auditors over ~26k money-path lines. NO Critical/High/Medium defects — the money core is sound (DD1/DD2/D4, reservation lifecycle, FC-I16 cursor resets, DTS/PID bounds, DB accounting all re-derived from code and verified). All net-new findings Low/Info. Structural: execute_rebalance is DEAD (live path = find_rebalance_candidates -> engine.run_cycle). Manifest drift: rebalancer.py/boltz_manager.py grew past their pinned blobs (re-manifest at Phase 8).

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-001 | Low | concurrency | modules/fee_controller.py:2842@3a3af0b | _hive_member_zero_fee_active reads _hive_member_set_at.get() lock-free; the P2-006 sweep converted the sibling read but missed this money-gate site. Atomic .get() (no crash), one-cycle zero-fee-gate flip, self-corrects. Fix: route through _cached_hive_membership_active. | FIXED | b8a6b57 | tests/test_p4_001_hive_zero_fee_locked_read.py |
| P4-002 | Low | accounting | modules/database.py:5678@07356f0 | _sanitize_fee clamps closure fee components to 50000 sats; a real large force-close (multi-HTLC sweep / spike) is truncated -> closure cost under-counted -> lifetime P&L over-states profit (optimistic direction). Reporting-only. Fix: use the _sanitize_amount (10 BTC) ceiling for closure fees. | FIXED | f052d30 | tests/test_p4_002_closure_fee_ceiling.py |
| P4-003 | Low | accounting | modules/database.py:3975@07356f0 | record_spend_event allows a negative amount_sats (only magnitude-clamped), which SUM()s subtract from committed spend -> remaining rises (overspend-permitting direction). No current caller passes negative -> latent. Fix: reject amount<=0 in record_spend_event. | FIXED | 0066a43 | tests/test_p4_003_record_spend_event_guard.py |
| P4-004 | Low | accounting | modules/database.py:4985@07356f0 | record_forward_and_reputation runs the peer_reputation success upsert unconditionally even when the forward INSERT OR IGNORE was a duplicate (startup-hydration vs live overlap) -> reputation double-counts on replay (fee is correctly deduped). Reputation feeds decisions -> money-adjacent. Fix: guard reputation upserts with if not duplicate. | FIXED | 4c6c2f8 | tests/test_p4_004_forward_reputation_dedup.py |
| P4-005 | Low | money-path/validation | modules/boltz_manager.py:1753@8db9376 | _validate_onchain_address (P1-006) is wired only into withdraw; loop_out address, chainswap to_address, refund/claim destination pass unvalidated (boltzd sole validator — the condition P1-006 judged insufficient). A wrong-network/typo'd address on a reverse swap sends proceeds to an unrecoverable dest. Operator-only. Fix: validate destination on all four paths. | FIXED | af61ace | tests/test_p4_005_boltz_destination_validation.py |
| P4-006 | Low | security/injection | modules/boltz_manager.py:2069@8db9376 | chainswap is the one createswap path missing the P1-015 -- end-of-options terminator; to_address is a flag value (int amount is a safe positional) so live risk is marginal, but it's the residual of the P1-015 class. Fix: add -- before positionals in chainswap. | FIXED | fa29114 | tests/test_p4_006_chainswap_terminator.py |
| P4-007 | Low | money-path/reservation | modules/rebalance_engine_v2.py:2890@e31a200 | On the engine path, payment_pending=True WITHOUT payment_hash marks the history row 'failed' while _finish_execution_budget holds the reservation active; reconcile_pending_settlements only scans 'pending_settlement' so it never releases -> one max_fee reservation stranded 'active'. Over-reserve (conservative); reaped by cleanup_stale_reservations + budget-window ageing. Fix: release (or mark sweepable) on the no-hash pending edge. MIRROR: release on no-hash pending; hold only sweepable (payment_hash) as pending_settlement. | FIXED | 0814fb0 | tests/test_p4_rebalance_hardening.py |
| P4-008 | Low | concurrency/double-pay | modules/rebalance_engine_v2.py:3461@e31a200 | _run_cycle_locked abandons a worker whose future.cancel() returns False after the 120s as_completed timeout, then run_cycle's finally releases _cycle_lock while the orphan is still in _execute_pair (reserving+paying); the next cycle can acquire the lock and, with no in-flight-dest guard, re-select the same dest -> second payment (double-pay + double budget). Same family as P1-013. Requires the 120s ceiling exceeded. Direction dangerous. Fix: don't release the cycle lock while a worker is in-flight, or add an in-flight-destination guard in find_candidates. In-flight-destination guard (dest_inflight skip reason); filter not wait, no deadlock/stall confirmed. | FIXED | 8f55006 | tests/test_p4_rebalance_hardening.py |
| P4-009 | Low | dead-code | modules/rebalancer.py:2354@32b22d2 | execute_rebalance's failure branch calls release_budget_reservation unconditionally even on payment_pending (violates hold-on-pending), but execute_rebalance is DEAD (find_rebalance_candidates always returns [], so the cl-revenue-ops.py:3220 loop is empty). Latent overspend window if ever re-wired. Fix: delete execute_rebalance or mirror the engine's hold-on-pending. MIRROR hold-on-pending (NOT deleted — live caller at cl-revenue-ops.py:3221; earlier 'dead' verdict was call-site-present/loop-empty). | FIXED | cd5d77e | tests/test_p4_rebalance_hardening.py |
| P4-010 | Info | correctness/consistency | modules/policy_manager.py:1237@3a3af0b | set_policies_batch treats explicit fee_ppm_target:None as CLEAR (dict.get) while set_policy treats it as KEEP; not a money bug (STATIC-without-target rejected on both) but a single->batch migration surprise. | OPEN | | |

## Phase 4 — coverage-gap fill (2026-07-02)

Remaining Tier-1 chunks (engine 401-1200/1601-2000, rebalancer 1-1600) read → 100% Tier-1 coverage. 3 more Low/Info, none causing overspend/wrong-payment.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-011 | Low | correctness/estimate-bias | modules/rebalancer.py:1745@32b22d2 | _get_last_hop_fee PRIORITY-2 gossip path computes base_ppm with no min(...,1_000_000) cap (the sibling PRIORITY-1 peer path caps at 1e6); garbage gossip base_fee inflates the inbound-fee estimate → over-estimates cost → SUPPRESSES spend (conservative direction, no overspend). Fix: cap at 1e6 like the peer path. Gossip base-fee ppm capped at 1e6 like the peer path. | FIXED | cb6ca36 | tests/test_p4_rebalance_hardening.py |
| P4-012 | Low | money-path/EV | modules/rebalance_engine_v2.py:84@e31a200 | _bounded_historical_fee_ppm treats max_fee_ppm==0 as "cap disabled" → _historical_fee_rate_ppm returns UNCAPPED ppm feeding destination_refill_value/source_drain_value (EV benefit terms); with max_fee_ppm=0 a freak historical fee ratio can inflate expected_future_value and clear the hold gate for a losing rebalance. Latent (degenerate config; P1-009 enforces min<=max but not max>0). Fix: treat max_fee_ppm=0 as no-historical-benefit (cap to 0) or enforce max_fee_ppm>0. max_fee_ppm==0 => zero historical benefit (return 0.0), not uncapped. | FIXED | 5b0e164 | tests/test_p4_rebalance_hardening.py |
| P4-013 | Info | money-path/design | modules/rebalance_engine_v2.py:498@e31a200 | The sats-EV hold gate is bypassed when route_cost_sats==0 (free intra-fleet route executes even if source_opportunity_sats exceeds refill value) — "can never lose to do-nothing" is not strictly true once opportunity cost is counted. Documented design (F2/F5 zero-budget equalization), bounded by upstream source gating. No action. | OPEN | | |

## Phase 4 — REOPENED by refutation gate (2026-07-02)

The refuter OVERTURNED the "no Medium+ in the money core" verdict: the DD1 unified-budget fix was INCOMPLETE. DD2 fee clamp, P4-002 closure ceiling, and P4-008 in-flight guard survive clean; DD1 and the reservation lifecycle do NOT. These complete the operator's already-approved DD1 ruling ("all categories incl. boltz/capex atomically") — fixes, not new decisions.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-014 | Medium | money-path/overspend | modules/boltz_manager.py:1473@a2e91f2 | Boltz auto-cycle (live daemon path) has NO atomic budget reservation: gates only on get_budget_status() whose non-boltz component uses the 30s MEMO (the P2-011 force_fresh fix only touched cro:6719, not the boltz gate), AND boltz writes nothing to the ledger so it is invisible to both atomic reserve paths. T3 reserves 90k live; T6 boltz reads stale ~100k-remaining memo and swaps 50k → 140k vs 100k budget. Same mechanism as P2-011 (842x soak) left uncovered on boltz. Overturns DD1. Fix: force_fresh on the boltz gate AND route boltz spend through an atomic cross-category reservation (completes DD1). Boltz reserve/settle/release via atomic unified budget + force_fresh gate; stress harness boltz reserver PASS. | FIXED | 198510f | tests/test_p4_014_boltz_unified_budget.py |
| P4-015 | Medium | money-path/overspend | modules/database.py:3681@cccca11 | cleanup_stale_reservations force-releases any active budget_reservations row older than reservation_timeout_hours (4h), running before every rebalance cycle (rebalancer.py:1278) + at startup — this DEFEATS the P4-007 hold-on-pending: a rebalance HTLC pending >4h has its reservation released while still live, the budget is re-reserved next cycle, and when the original settles mark_budget_spent no-ops (row already released) → same budget spent twice. Fix: cleanup must NOT release a reservation tied to a still-pending_settlement payment (skip/extend for pending). cleanup_stale skips reservations linked to pending_settlement rows (reservation_id==rebalance_history.id); no leak. | FIXED | 6144b9b | tests/test_p4_015_pending_reservation_hold.py |
| P4-016 | Low | money-path/double-count | cl-revenue-ops.py:7370@76e7c52 | On the rebalance reserve path, budget_limit = effective_budget - non_rebalance_costs already subtracts the generic ledger + open/close, yet _reserve_budget_atomic ALSO adds raw SUM(spend_events incl channel_open/close) + SUM(spend_reservations) into daily_committed → generic counted 2x, open/close subtracted-and-added. Direction conservative (rebalance-budget starvation, not overspend) but refutes the clean-partition claim; can zero the rebalance budget on a capex-active node. Fix: don't double-count (subtract OR add, not both). Full effective budget passed; atomic BEGIN IMMEDIATE sum is sole authoritative count. | FIXED | db1e510 | tests/test_p4_016_budget_double_count.py |
| P4-017 | Low | money-path/window | modules/database.py:140@cccca11 | _reserve_budget_atomic filters active budget_reservations by reserved_at >= since_timestamp, so a still-active reservation older than the daily/weekly window drops out of committed → under-counts held budget in the OVERSPEND direction (compounds P4-015). The generic reserve_spend path correctly sums active reservations with no time filter. Active reservations are currently-held budget and should count regardless of age. Fix: remove the age filter for active reservations. Age filter dropped for active-reservation sums (kept for spent/cost windows). | FIXED | 6e46902 | tests/test_p4_017_active_reservation_window.py |

## Phase 4 — re-refutation pass 2 (2026-07-02): capex path still open

Re-refuter CONFIRMED boltz (P4-014), rebalance sum (P4-016/017), pending-hold (P4-015), and the stress instrument are all sound — but found the SAME memo+non-atomic-reserve defect a THIRD time, on the capex path DD1 explicitly scoped. Phase 4 stays open.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-018 | Medium | money-path/overspend | modules/capacity_planner.py:3311@0f342fc | Capacity planner (T7 daemon, planner_dry_run defaults false) autonomously opens/closes channels: _execute_open (:3311) and _execute_close (:3664) call db.reserve_spend(category=channel_open/close, ...) WITHOUT effective_budget_sats -> enforce_budget=False -> the atomic cross-category rejection block is SKIPPED (always inserts, never rejects on budget). The only gate is _check_unified_budget reading _total_cost_budget_status() with NO force_fresh (cro:7347) = 30s stale memo. Identical to P2-011/P4-014, still live for capex, in DD1 scope. T3 reserves 90k atomically; within 30s T7 reads stale headroom, opens a channel -> rebalance+channel_open jointly exceed the unified budget. Fix: pass effective_budget_sats to reserve_spend on open/close AND force_fresh the capex gate (same treatment as P4-014 boltz). _execute_open/_execute_close reserve atomically w/ effective_budget_sats + force_fresh gate; close reserves BEFORE the RPC. Complete spend-site enumeration confirms all 4 daemon spenders covered. | FIXED | 77dfc57 | tests/test_p4_018_planner_unified_budget.py |
| P4-019 | Low | money-path/lost-write | modules/capex_budget.py:465@cf933f6 | settle_boltz_swap_reservation calls record_boltz_spend (which catches Exception->returns False, swallowing the P2-008 loud/retry) then release_boltz_swap_reservation UNCONDITIONALLY, ignoring the return. A created swap whose spend_events write persistently fails -> reservation released, no spend event -> committed cost invisible to budget (overspend-permitting). Re-opens P2-008 requirement on the boltz settle path. Fix: don't release if the spend-event write failed (retry/propagate like P2-008). settle releases only after the spend-event write succeeds (loud/retry per P2-008). | FIXED | bddeffc | tests/test_p4_019_boltz_settle_loud_write.py |

## Phase 4 — re-refutation pass 3 (2026-07-02): 5th spender + capex settle

Pass 3 CONFIRMED the atomic sum is exact and the 4 reserving paths (rebalance/boltz/open/close) are closed — but found a 5th autonomous spender with NO reservation, and a capex-settle lost-write. Phase 4 stays open. DEFINITIVE fix + permanent enumeration guard to follow.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-020 | Medium | money-path/overspend | modules/rebalancer.py:2533@1bb756a | Defibrillation (diagnostic shock, T7 daemon via execute_cycle->_execute_defibrillation->diagnostic_rebalance->execute_candidate(reserve_budget=False)) takes NO atomic reservation; its only gate _check_capital_controls (rebalancer.py:2733/2790) counts actual-SPENT only and is blind to all reservations -> sees spent~0, passes, commits a shock (fee clamped to min(diagnostic_rebalance_max_fee_sats=400, daily_budget_sats, 10000), 1/cycle) on top of a fully-reserved cross-category budget. Same defect class as P4-014/018, 5th spender. Bounded (D4 cap, 1/cycle) so Medium-bounded not the earlier unbounded memo defect. Fix: route diagnostic shocks through the atomic rail (reserve_budget=True, category=rebalance) OR make _check_capital_controls reservation-aware for this path. Atomic-reserve: reserve_budget threaded through execute_candidate; diagnostic shock rejected when the cross-category budget is exhausted (local_budget_block -> shock_status blocked). Enumeration guard tests/test_all_spenders_atomic.py now enforces all spenders. | FIXED | 3aa72b1 | tests/test_p4_020_defibrillation_unified_budget.py |
| P4-021 | Low | money-path/lost-write | modules/capacity_planner.py:3407@43f84fd | Capex open (:3407) / close (:3751) settle calls mark_spend_reservation_spent inside try/except: pass, ignoring the return (P4-019 gap not covered on capex). On a persistent spend_events write failure the reservation stays active but nothing retries, and cleanup_stale_spend_reservations (cro:7241, 4h, no category filter) then releases it with no spend_event -> committed open/close fee vanishes from the atomic rail (the rail counts opens/closes ONLY via spend_events, not the canonical cost tables). Overspend-permitting. Fix: mirror P4-019 (check return/retry) OR protect capex reservations from the stale sweep like P4-015 protects pending_settlement. _settle_capex_reservation loud/retry (never releases on persistent write fail) + cleanup_stale skips channel_open/close (blind sweep). | FIXED | 1fb9b55 | tests/test_p4_021_capex_settle_lost_write.py |

## Phase 4 — pass-3 fix residual closed (2026-07-02)

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-022 | Low | money-path/lost-write | modules/database.py:4066@c231a58 | boltz reservations held active by P4-019 loud-write were still subject to the blind stale sweep (same vanish vector P4-021 closed for capex); journal re-settle not guaranteed within 4h. Added 'boltz' to the blind-sweep-protected categories; explicit category sweep still reaches it. Flagged by the P4-020/021 fix agent, closed proactively before pass-4 refutation. | FIXED | HEAD | tests/test_p4_022_boltz_blind_sweep.py |

## Phase 4 — re-refutation pass 4 (2026-07-02): the guard itself was unsound

Pass 4 attacked the enumeration guard and proved it is NOT a complete enumeration — only an allowlist of already-known patterns. It missed a 6th swap-create (chainswap, unreserved) and is blind to computed-method rpc.call + the money-RPC family. Phase 4 stays open. The fix must make the guard an HONEST MIRROR.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-023 | Medium | money-path/overspend | modules/boltz_manager.py:2243@e899d61 | chainswap (createchainswap, boltz_manager.py:2274) is the 6th swap-create and the ONLY one with no atomic pre-create reservation: sole gate _enforce_budget_for_quote (:1519) is a soft advisory read (no reserve_spend/BEGIN IMMEDIATE, no-op if enforce_budget=false); cost recorded post-hoc after an up-to-180s subprocess. Concurrent operator chainswap + daemon reserves (T3/T6/T7) → the in-flight fee is invisible to the atomic total for up to 180s → cross-category overspend (pre-P4-014 defect class, uncovered on chainswap). Operator-triggered but the overspend lands on the daemon side; DD1 scopes boltz cost regardless of trigger. Fix: give chainswap the P4-014 _open_swap_budget_reservation treatment before createchainswap. _open_swap_budget_reservation before createchainswap (rejects on unified-budget exhaustion); settle/release in try/finally. | FIXED | c3b3c8e | tests/test_p4_023_chainswap_unified_budget.py |
| P4-024 | Medium | test-integrity | tests/test_all_spenders_atomic.py:52@campaign | The enumeration guard is NOT a complete enumeration — its markers are an allowlist of KNOWN patterns (_SWAP_LITERALS = createswap/createreverseswap only; RPC markers fundchannel/close only). It never emits a site for createchainswap (P4-023 invisible), and is structurally blind to computed-method rpc.call(method,...) (rebalance_native_executor_v2._rpc_call passes sendpay as a variable) and the whole money-RPC family (withdraw/multifundchannel/splice_*/txsend/sendpay/pay/sendonion/keysend). Gives FALSE assurance of completeness — the exact "one more spender" failure it was built to prevent. Fix: broaden the scanner to flag createchainswap, computed-method rpc.call, and the full money-RPC method set, each requiring an allowlist coverage class (incl. not-a-spend/rail-counted with justification), so the guard is an honest mirror. Scanner rebuilt as complete enumeration: any create* boltz cmd + full money-RPC set + computed-method rpc.call; coverage classes; 3 test-the-test injections trip it. | FIXED | 33b8166 | tests/test_all_spenders_atomic.py |
| P4-025 | Low | money-path/window | modules/rebalance_engine_v2.py:2879@91f7f42 | Defibrillation SUCCESS marks the reservation spent (mark_budget_spent, drops it from the active-reservation sum) BEFORE the rebalancer writes record_rebalance_cost — a transient window where the shock fee is counted by neither (overspend direction), unlike the auto-cycle which records cost before marking spent. Bounded to the D4 cap (<=400 sats, 1/cycle). Fix: record the cost before mark_budget_spent (match the auto-cycle ordering). account_costs=True threaded; defib records rebalance_cost before mark_budget_spent (matches auto-cycle); no double-count. | FIXED | ab2c751 | tests/test_p4_025_defibrillation_cost_ordering.py |

## Phase 4 — re-refutation pass 5 (2026-07-02): overspend CLOSED; guard still evadable

Pass 5 CONFIRMED (A) cross-category overspend is CLOSED on all 6 live daemon spenders (rebalance/boltz loop_in+loop_out+chainswap/capex open+close/defibrillation — all atomic, sum exact, no leak/double-count). But (B) the enumeration guard is still not a complete mirror. These are TEST-INTEGRITY findings (no runtime/money change).

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-026 | Medium | test-integrity | tests/test_all_spenders_atomic.py:139@campaign | The rebuilt guard models rpc.call("m") and rpc.call(computed) but is blind to pyln ATTRIBUTE-style dispatch rpc.<method>(...). Real instance: data_service.send_pay -> self._plugin.rpc.sendpay(route, payment_hash) (data_service.py:320) emits ZERO sites and passes the guard green — an unreserved sendpay. No live caller today (send_pay is dormant; live rebalance payment uses the covered _rpc_call dynamic site), but wiring send_pay into any daemon ships a live un-reserved spender with a GREEN guard — the exact failure the guard exists to prevent. The test-the-test shares the blind spot (never injects the attribute form). Fix: flag Call whose func.attr in the money-RPC set; classify data_service.send_pay (not-a-spend transport); add a 4th attribute-form test-the-test injection. Scanner detects attribute-style rpc.<method>() dispatch (rpc-handle receivers); data_service.send_pay allowlisted not-a-spend (dormant); 4th test-the-test injection trips. | FIXED | 380c14f | tests/test_all_spenders_atomic.py |
| P4-027 | Low | test-integrity | tests/test_all_spenders_atomic.py:145@campaign | Sites are a set keyed on (filename, enclosing_function, marker) with no line/count identity, so N same-marker calls in one function collapse to one tuple — a SECOND unreserved money call sharing an existing (file,func,marker) with an allowlisted covered site is absorbed with no trip (e.g. a 2nd rpc.call("close") in _rpc_close). Fix: key sites on line number (or count per (file,func,marker)) so an added same-marker call surfaces. Counter-keyed sites + expected-count in allowlist; count_mismatch failure axis; duplicate-marker injection trips. | FIXED | 380c14f | tests/test_all_spenders_atomic.py |

## Phase 4 — re-refutation pass 6 (2026-07-02): money CLOSED; final guard tightening

Pass 6: (A) cross-category overspend CLOSED on all 6 live spenders (money code byte-identical to pass-5-certified state). (B) guard is a complete correct mirror of today's code; one theoretical residual (Low, no live instance) — closed by a forbid-pattern lint.

| ID | severity | dimension | file:line@blob | description | status | fix_commit | test |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P4-028 | Low | test-integrity | tests/test_all_spenders_atomic.py:140@campaign | The attribute-dispatch detector only fires when the receiver is syntactically `rpc` or an attr chain ending `.rpc`; an ALIASED handle evades — `h=self.rpc; h.sendpay(...)`, `getattr(self.rpc,'sendpay')(...)`, `m=self.rpc.sendpay; m(...)`, `p=self._plugin.rpc; p.pay(...)`. NO live instance exists (grep: zero rpc-handle aliases / bound-method stashes of money methods), so no live money impact — theoretical guard gap only. Fix (complete, no dataflow needed): add a forbid-pattern lint that FAILS if any runtime source aliases an rpc handle to a local, getattr's a money method off an rpc handle, or stashes rpc.<moneymethod> as a bound method — so a future introduction of the evasion trips the guard. | OPEN | | |
