#!/bin/sh
# Launch cl_revenue_ops from the isolated Python environment installed in a
# Grand Prix CLN container. The pinned lab image has no compatible system
# Python environment, so relying on /usr/bin/env python3 would make fresh runs
# differ from the frozen contender environment.
set -eu

PLUGIN_ROOT="${CL_REVENUE_OPS_ROOT:-/opt/cl_revenue_ops}"
export PYTHONPATH="$PLUGIN_ROOT${PYTHONPATH:+:$PYTHONPATH}"
exec "$PLUGIN_ROOT/.venv/bin/python" "$PLUGIN_ROOT/cl-revenue-ops.py" "$@"
